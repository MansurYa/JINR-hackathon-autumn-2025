# JINR Patent Landscape Analyzer

Проект для хакатона JINR Hackathon Autumn 2025 по созданию патентного ландшафта на основе объектов интеллектуальной собственности (патенты, статьи, базы данных, ПО) в областях деятельности ОИЯИ.

## 📋 Описание проекта

### Цель
Создать автоматизированную систему анализа патентного ландшафта, которая:
1. ✅ Извлекает научные области (теги) из документов с помощью LLM
2. ✅ Преобразует теги в векторные представления (эмбединги)
3. ✅ Кластеризует документы по научным областям
4. 🔄 Визуализирует патентный ландшафт (TODO)

### Подход
**LLM → Теги → Эмбединги → Кластеризация → Визуализация**

### Текущий статус
- ✅ **ЭТАП 0:** Присвоение ID документам
- ✅ **ЭТАП 1:** Извлечение тегов с помощью LLM (OpenRouter API, GPT-4o-mini)
- ✅ **ЭТАП 2:** Генерация эмбедингов тегов (BAAI/bge-base-en-v1.5 с префиксом "Scientific theme:")
- ✅ **ЭТАП 3:** Генерация эмбедингов документов (weighted sum тегов)
- ✅ **ЭТАП 4:** Иерархическая кластеризация (Dendrogram + UMAP визуализация)
- ✅ **ЭТАП 5:** Интерактивная 3D визуализация патентного ландшафта (Dash + Plotly)

## 🏗️ Архитектура

### Структура проекта

```
JINR-hackathon-autumn-2025/
├── config.json                 # Конфигурация (API ключи, модели)
├── prompt.txt                  # Системный промпт для LLM
├── requirements.txt            # Зависимости Python
├── README.md                   # Документация проекта
│
├── src/                        # Основной код
│   ├── document_saver.py                  # Утилиты для сохранения документов
│   ├── embeddings_generator.py            # Генератор эмбедингов (BAAI/bge)
│   ├── openrouter_client.py               # Клиент OpenRouter API
│   ├── id_assigner.py                     # ЭТАП 0: Присвоение ID документам
│   ├── tag_extractor.py                   # ЭТАП 1: Извлечение тегов с помощью LLM
│   ├── tag_embeddings_generator.py        # ЭТАП 2: Генерация эмбедингов тегов
│   ├── document_embeddings_generator.py   # ЭТАП 3: Генерация эмбедингов документов
│   ├── hierarchical_clustering.py         # ЭТАП 4: Иерархическая кластеризация
│   └── visualization/                     # ЭТАП 5: Интерактивная визуализация
│       ├── generate_clustering_cache.py   # Генерация кэша кластеризации
│       ├── app.py                         # Dash веб-приложение
│       └── README.md                      # Документация визуализации
│
├── temporary_data/             # Данные
│   ├── fake_data.jsonl                # Исходные документы с ID
│   ├── fake_data_tags.jsonl           # Теги с весами для каждого документа
│   ├── unique_tag_embeddings.npz      # Эмбединги уникальных тегов
│   ├── document_embeddings.npz        # Эмбединги документов
│   ├── clustering_results.json        # Результаты кластеризации
│   ├── dendrogram.png                 # Дендрограмма
│   └── umap_clusters.png              # UMAP визуализация кластеров
│
└── temporary_scripts/          # Тестовые скрипты
    ├── test_embeddings.py             # Тесты генератора эмбедингов
    ├── test_tag_embeddings.py         # Тесты эмбедингов тегов
    └── test_clustering.py             # Тесты кластеризации
```

## 🔄 Пайплайн обработки данных

### ЭТАП 0: Присвоение уникальных ID документам

**Файл:** `src/id_assigner.py`

**Цель:** Присвоить уникальный ID каждому документу в JSONL файле.

**Алгоритм:**
1. **Первый проход:** Читаем файл построчно, собираем все существующие ID в множество
2. **Второй проход:** Для каждого документа без ID:
   - Находим минимальный свободный ID (заполняем "дырки")
   - Добавляем ID к документу
   - Записываем во временный файл
3. Заменяем исходный файл временным

**Входные данные:**
```json
{"name": "Документ", "text_of_document": "Текст..."}
```

**Выходные данные:**
```json
{"id": 0, "name": "Документ", "text_of_document": "Текст..."}
```

**Команда запуска:**
```bash
cd src
python id_assigner.py
```

---

### ЭТАП 1: Извлечение тегов с помощью LLM

**Файл:** `src/tag_extractor.py`

**Цель:** Извлечь научные области (теги) из каждого документа с весами важности.

**Алгоритм:**
1. Читаем документы из `fake_data.jsonl`
2. Для каждого документа:
   - Формируем запрос к LLM: `prompt.txt` + `name` + `text_of_document`
   - LLM возвращает теги с весами в формате: `Tag1: 0.6, Tag2: 0.3, Tag3: 0.1`
   - Парсим теги и веса
   - Проверяем, что теги на английском языке
   - Нормализуем веса, чтобы сумма = 1.0
   - Сохраняем в отдельный файл `fake_data_tags.jsonl`

**LLM модель:** OpenRouter API (`openai/gpt-4o-mini`)
**Температура:** 0.0 (детерминированные результаты)

**Требования к тегам:**
- Только английский язык
- 1-6 слов
- Первое слово с заглавной буквы
- Веса от 0.0 (не включая) до 1.0
- Сумма весов = 1.0

**Примеры тегов:**
- Machine learning
- High energy physics
- Nuclear reactions
- Quantum chromodynamics
- Convolutional neural networks

**Входные данные:**
```json
{"id": 0, "name": "Методы оптимизации...", "text_of_document": "В статье..."}
```

**Выходные данные:**
```json
{"document_id": 0, "tags": {"Machine learning": 0.6, "Neural networks": 0.3, "Optimization methods": 0.1}}
```

**Команда запуска:**
```bash
cd src
python tag_extractor.py
```

---

### ЭТАП 2: Генерация эмбедингов для уникальных тегов

**Файл:** `src/tag_embeddings_generator.py`

**Цель:** Создать векторные представления (эмбединги) для всех уникальных тегов.

**Алгоритм:**
1. Собираем все уникальные теги из `fake_data_tags.jsonl` в множество
2. Сортируем теги (для детерминированности)
3. Генерируем эмбединги батчами (batch_size=16 для Mac M1):
   - Используем модель `BAAI/bge-base-en-v1.5` (768-мерные эмбединги)
   - Устройство: MPS (Mac M1) / CUDA / CPU
   - Нормализация включена (для косинусного сходства)
4. Сохраняем в компактный формат `.npz`:
   - Массив тегов (строки)
   - Массив эмбедингов (float32)

**Модель эмбедингов:** `BAAI/bge-base-en-v1.5`
**Размерность эмбедингов:** 768
**Формат хранения:** NumPy compressed (.npz)

**Результат:**
- 27 уникальных тегов
- Эмбединги (27, 768)
- Размер файла: ~71 KB

**Выходные данные:** `unique_tag_embeddings.npz`
```python
{
    'tags': ['Accelerator Physics', 'Artificial Intelligence', ...],
    'embeddings': array([[...], [...], ...], dtype=float32)
}
```

**Команда запуска:**
```bash
cd src
python tag_embeddings_generator.py
```

---

### ЭТАП 3: Создание векторных представлений документов

**Файл:** `src/document_embeddings_generator.py`

**Цель:** Создать эмбединг для каждого документа на основе его тегов и весов.

**Алгоритм:**
1. **Загружаем предпосчитанные эмбединги тегов** из `unique_tag_embeddings.npz`
2. **Загружаем теги документов** из `fake_data_tags.jsonl`
3. Для каждого документа:
   - Берем теги с весами (например: `{"Machine learning": 0.6, "Neural networks": 0.3, ...}`)
   - Вычисляем линейную комбинацию эмбедингов:
     ```
     doc_embedding = Σ(tag_embedding_i * weight_i)
     ```
   - Нормализуем результат: `doc_embedding = doc_embedding / ||doc_embedding||`
4. **Сохраняем** в `document_embeddings.npz` (формат: document_ids + embeddings)

**Формат хранения:** NumPy .npz (compressed)
**Структура файла:**
```python
{
    'document_ids': [0, 1, 2, ...],  # ID документов
    'embeddings': array([[...], [...], ...], dtype=float32)  # Эмбединги (N, 768)
}
```

**Пример вычисления:**
```python
# Документ с тегами
doc_tags = {
    "Machine learning": 0.6,
    "Neural networks": 0.3,
    "Optimization methods": 0.1
}

# Вычисление эмбединга документа
doc_emb = (emb["Machine learning"] * 0.6 +
           emb["Neural networks"] * 0.3 +
           emb["Optimization methods"] * 0.1)
doc_emb = doc_emb / np.linalg.norm(doc_emb)  # Нормализация
```

**Входные данные:** `fake_data_tags.jsonl`
```json
{"document_id": 0, "tags": {"Machine learning": 0.6, "Neural networks": 0.3, "Optimization methods": 0.1}}
```

**Выходные данные:** `document_embeddings.npz`
```python
{
    'document_ids': np.array([0, 1, 2, ...]),
    'embeddings': np.array([[...], [...], ...])  # (10, 768)
}
```

**Команда запуска:**
```bash
cd src
python document_embeddings_generator.py
```

---

### ЭТАП 4: Кластеризация документов

**Файл:** `src/hierarchical_clustering.py`

**Цель:** Сгруппировать документы по научным областям с помощью иерархической кластеризации.

**Метод кластеризации:** Hierarchical Agglomerative Clustering
**Метрика:** Cosine distance
**Linkage:** Average (UPGMA)

**Алгоритм:**
1. **Загружаем данные:**
   - Эмбединги документов из `document_embeddings.npz`
   - Теги документов из `fake_data_tags.jsonl`

2. **Строим дендрограмму:**
   - Визуализация иерархической структуры кластеров
   - Сохраняется в `dendrogram.png`
   - Помогает выбрать оптимальное количество кластеров

3. **Интерактивный выбор параметров:**
   - Вариант 1: Указать количество кластеров (n_clusters)
   - Вариант 2: Указать порог расстояния (distance_threshold)

4. **Кластеризация:**
   - Применяем Agglomerative Clustering с выбранными параметрами
   - Получаем метки кластеров для каждого документа

5. **Naming кластеров (Weighted Tag Frequency):**
   - Для каждого кластера суммируем веса тегов по всем документам
   - Сортируем теги по убыванию суммарного веса
   - Название кластера = топ-3 тега через запятую

   **Пример:**
   ```python
   Кластер 0 (3 документа):
     Doc 0: {"Machine learning": 0.6, "Neural networks": 0.3, "Optimization": 0.1}
     Doc 1: {"Machine learning": 0.5, "Deep learning": 0.4, "AI": 0.1}
     Doc 2: {"Neural networks": 0.7, "Deep learning": 0.2, "Vision": 0.1}

   Суммарные веса:
     Machine learning: 1.1 ⬅ топ-1
     Neural networks: 1.0  ⬅ топ-2
     Deep learning: 0.6    ⬅ топ-3

   Название: "Machine learning, Neural networks, Deep learning"
   ```

6. **Оценка качества кластеризации:**
   - **Silhouette Score** (от -1 до 1, чем больше - тем лучше)
   - **Davies-Bouldin Index** (чем меньше - тем лучше)
   - **Calinski-Harabasz Score** (чем больше - тем лучше)

7. **UMAP визуализация:**
   - Снижение размерности 768D → 2D
   - График с раскрашенными кластерами
   - Подписи ID документов
   - Сохраняется в `umap_clusters.png`

**Входные данные:**
- `document_embeddings.npz` — эмбединги документов (10, 768)
- `fake_data_tags.jsonl` — теги с весами для каждого документа

**Выходные данные:**
- `clustering_results.json` — результаты кластеризации (названия, ID документов, топ-теги)
- `dendrogram.png` — дендрограмма
- `umap_clusters.png` — 2D визуализация кластеров

**Команда запуска (интерактивный режим):**
```bash
cd src
python hierarchical_clustering.py
```

**Команда запуска (тестовый режим):**
```bash
cd temporary_scripts
python test_clustering.py --mode single        # 3 кластера
python test_clustering.py --mode multiple      # 2, 3, 4, 5 кластеров
```

---

### ЭТАП 5: Интерактивная 3D визуализация патентного ландшафта ✅

**Файлы:**
- `src/visualization/generate_clustering_cache.py` — генерация кэша кластеризации
- `src/visualization/app.py` — Dash веб-приложение
- `run_visualization.sh` — скрипт автоматического запуска

**Цель:** Создать интерактивное веб-приложение для исследования патентного ландшафта на разных уровнях детализации.

**Реализованные компоненты:**

1. **Кэширование всех уровней кластеризации:**
   - Генерация 79 уровней (от 1 до 1948 кластеров) один раз
   - Сохранение в `data/clustering_cache.json` (~800 MB)
   - Быстрое переключение между уровнями без пересчёта
   - Для каждого кластера: центроид, цвет, название, топ-теги, количество документов

2. **Интерактивная 3D визуализация:**
   - **Технологии:** Dash + Plotly + Bootstrap
   - **3D навигация:** вращение, масштабирование, перемещение
   - **t-SNE проекция:** снижение размерности 1024D → 3D для визуализации центроидов
   - **Размер сфер:** динамический расчёт по формуле (3 варианта на выбор):
     - Формула 1: `√n × log(n) / scale`
     - Формула 2: `∛n / scale`
     - Формула 3: `log(n) / scale`
   - **Цвета:** наследование от родительских кластеров в иерархии

3. **Интерфейс (3 панели):**
   - **Левая панель (25%):** список всех кластеров с цветами и названиями
   - **Центральная панель (50%):** 3D визуализация кластеров-сфер
   - **Правая панель (25%):**
     - Логарифмический слайдер количества кластеров (1-1948)
     - Слайдер масштаба сфер (10-200)
     - Выбор формулы расчёта радиуса (dropdown)
     - Информация о выбранном кластере (клик на сферу)

4. **Адаптивные названия кластеров:**
   - < 5 кластеров: топ-3 тега + "..."
   - 5-9 кластеров: топ-2 тега + "..."
   - ≥ 10 кластеров: топ-1 тег

**Команда запуска:**
```bash
# Автоматический запуск (рекомендуется)
./run_visualization.sh

# Ручной запуск
cd src/visualization
python3 generate_clustering_cache.py  # Один раз
python3 app.py                         # Каждый раз

# Открыть в браузере: http://localhost:8050
```

**Производительность:**
- Генерация кэша: ~5-10 минут (один раз)
- Загрузка кэша: ~2-3 секунды
- Переключение уровней: <100 мс
- RAM: ~2 GB (приложение + кэш)

## 📊 Формат данных

### Исходные документы (fake_data.jsonl)
```json
{
  "id": 0,
  "name": "Название документа",
  "type_of_document": "article|patent|databases|software",
  "date": "YYYY.MM.DD",
  "authors": "Авторы через запятую",
  "identifier": "DOI или другой идентификатор",
  "text_of_document": "Текст документа..."
}
```

### Теги с весами (fake_data_tags.jsonl)
```json
{
  "document_id": 0,
  "tags": {
    "Machine learning": 0.6,
    "High energy physics": 0.2,
    "Computational physics": 0.15,
    "Nuclear reactions": 0.05
  }
}
```

### Эмбединги тегов (unique_tag_embeddings.npz)
```python
{
    'tags': np.array(['Tag1', 'Tag2', ...]),           # 27 уникальных тегов
    'embeddings': np.array([[...], [...], ...], dtype=float32)  # (27, 768)
}
```

### Эмбединги документов (document_embeddings.npz)
```python
{
    'document_ids': np.array([0, 1, 2, ...], dtype=int32),  # ID документов
    'embeddings': np.array([[...], [...], ...], dtype=float32)  # (10, 768)
}
```

### Результаты кластеризации (clustering_results.json)
```json
{
  "0": {
    "name": "Artificial Intelligence, Natural Language Processing, Machine learning",
    "top_tags": [
      ["Artificial Intelligence", 1.4],
      ["Natural Language Processing", 1.1],
      ["Machine learning", 0.6]
    ],
    "doc_count": 6,
    "doc_ids": [0, 1, 4, 7, 8, 9]
  },
  "1": { ... }
}
```

## ⚙️ Конфигурация

### config.json
```json
{
    "api_key": "your-openrouter-api-key",
    "model_name": "openai/gpt-4o-mini",
    "temperature": 0.0,
    "max_response_tokens": 2048,
    "embedding_model": "BAAI/bge-base-en-v1.5"
}
```

### prompt.txt
Системный промпт для LLM, который:
- Определяет задачу (извлечение научных областей)
- Задает формат вывода (Tag: weight, ...)
- Требует только английские теги
- Указывает, что сумма весов = 1.0

## 🚀 Установка и запуск

> **Быстрый старт:** См. [QUICKSTART.md](QUICKSTART.md) для немедленного запуска визуализации!

### Требования
- Python 3.11
- Mac M1 (MPS) / GPU (CUDA) / CPU
- ~2 GB RAM для визуализации

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Быстрый запуск визуализации
```bash
./run_visualization.sh
# Откройте в браузере: http://localhost:8050
```

### Зависимости
```
torch>=2.0.0                      # PyTorch для эмбедингов
sentence-transformers>=2.2.0      # BAAI/bge модели
transformers[torch]>=4.30.0       # Трансформеры
numpy>=1.24.0                     # Работа с массивами
scipy>=1.10.0                     # Иерархическая кластеризация
scikit-learn>=1.3.0               # Метрики качества кластеризации
matplotlib>=3.7.0                 # Визуализация дендрограммы
umap-learn>=0.5.0                 # UMAP снижение размерности
requests>=2.31.0                  # HTTP запросы
dash>=2.14.0                      # Dash веб-фреймворк
dash-bootstrap-components>=1.5.0  # Bootstrap компоненты для Dash
plotly>=5.17.0                    # Plotly для 3D визуализации
```

**Примечание:** OpenRouter API используется через прямые HTTP запросы (requests), не требует отдельной библиотеки openai.

### Запуск полного пайплайна (по порядку)

**1️⃣ ЭТАП 0: Присвоение ID**
```bash
cd src
python id_assigner.py
# Результат: fake_data.jsonl с ID для каждого документа
```

**2️⃣ ЭТАП 1: Извлечение тегов с помощью LLM**
```bash
cd src
python tag_extractor.py
# Результат: fake_data_tags.jsonl (теги с весами для каждого документа)
# Требует: API ключ OpenRouter в config.json
```

**3️⃣ ЭТАП 2: Генерация эмбедингов тегов**
```bash
cd src
python tag_embeddings_generator.py
# Результат: unique_tag_embeddings.npz (27 тегов, 768-мерные эмбединги)
# Использует: BAAI/bge-base-en-v1.5 с префиксом "Scientific theme:"
```

**4️⃣ ЭТАП 3: Генерация эмбедингов документов**
```bash
cd src
python document_embeddings_generator.py
# Результат: document_embeddings.npz (10 документов, 768-мерные эмбединги)
# Алгоритм: взвешенная сумма эмбедингов тегов
```

**5️⃣ ЭТАП 4: Иерархическая кластеризация**
```bash
# Интерактивный режим (с выбором параметров):
cd src
python hierarchical_clustering.py

# Тестовый режим (автоматический):
cd temporary_scripts
python test_clustering.py --mode single     # 3 кластера
python test_clustering.py --mode multiple   # 2, 3, 4, 5 кластеров

# Результат:
# - clustering_results.json (названия кластеров, ID документов, топ-теги)
# - dendrogram.png (дендрограмма для выбора количества кластеров)
# - umap_clusters.png (2D визуализация кластеров)
```

**6️⃣ ЭТАП 5: Интерактивная 3D визуализация**
```bash
# Автоматический запуск (рекомендуется)
./run_visualization.sh

# Ручной запуск:
# Шаг 1: Генерация кэша (один раз, ~5-10 минут)
cd src/visualization
python3 generate_clustering_cache.py
# Результат: data/clustering_cache.json (~800 MB, 79 уровней кластеризации)

# Шаг 2: Запуск веб-приложения
python3 app.py
# Открыть в браузере: http://localhost:8050
```

**Возможности визуализации:**
- **3 панели:** список кластеров (слева) + 3D визуализация (центр) + настройки (справа)
- **Интерактивное 3D пространство:** кластеры-сферы с навигацией (вращение, масштабирование, перемещение)
- **Логарифмический слайдер:** выбор количества кластеров (1-1948)
- **Динамический масштаб:** слайдер для изменения размера сфер (10-200)
- **3 формулы радиуса:** `√n×log(n)`, `∛n`, `log(n)` (выбор через dropdown)
- **Клик на кластер:** детальная информация (топ-5 тегов, количество документов)
- **Наследование цветов:** от родительских кластеров в иерархии

### Тестирование

**Тест генератора эмбедингов:**
```bash
cd temporary_scripts
python test_embeddings.py
```

**Тест эмбедингов тегов:**
```bash
cd temporary_scripts
python test_tag_embeddings.py
```

**Тест кластеризации:**
```bash
cd temporary_scripts
python test_clustering.py --mode single     # 3 кластера
python test_clustering.py --mode multiple   # 2, 3, 4, 5 кластеров
```

## 📈 Результаты

### Статистика обработки (на синтетическом датасете)

| Этап | Описание | Результат |
|------|----------|-----------|
| ЭТАП 0 | Присвоение ID | 10 документов с ID |
| ЭТАП 1 | Извлечение тегов | 10 документов → 27 уникальных тегов |
| ЭТАП 2 | Генерация эмбедингов тегов | 27 тегов → эмбединги (27, 768) с префиксом "Scientific theme:" |
| ЭТАП 3 | Генерация эмбедингов документов | 10 документов → эмбединги (10, 768) |
| ЭТАП 4 | Кластеризация документов | 3 кластера (Silhouette: 0.334) |

### Примеры извлеченных тегов

**Документ 0** (Machine Learning):
- Machine learning: 0.6
- Neural networks: 0.3
- Optimization methods: 0.1

**Документ 2** (High Energy Physics):
- High energy physics: 0.7
- Particle physics: 0.2
- Experimental data: 0.1

**Документ 5** (Nuclear Physics):
- Nuclear reactions: 0.6
- Monte Carlo methods: 0.3
- High energy physics: 0.1

### Результаты кластеризации (3 кластера)

**Кластер 0: "Artificial Intelligence, Natural Language Processing, Machine learning"**
- Количество документов: 6
- Документы: [0, 1, 4, 7, 8, 9]
- Топ теги: AI (1.4), NLP (1.1), ML (0.6)

**Кластер 1: "High Energy Physics, Nuclear reactions, Materials Science"**
- Количество документов: 3
- Документы: [2, 5, 6]
- Топ теги: High Energy Physics (0.7), Nuclear reactions (0.6), Materials Science (0.5)

**Кластер 2: "Distributed Computing Systems, Data Transmission Optimization"**
- Количество документов: 1
- Документы: [3]
- Топ теги: Distributed Computing (0.6), Data Transmission (0.3)

**Метрики качества:**
- Silhouette Score: 0.334
- Davies-Bouldin Index: 1.037
- Calinski-Harabasz Score: 2.82

### Косинусное сходство между тегами

| | Machine learning | Artificial Intelligence | High energy physics | Nuclear reactions |
|---|---|---|---|---|
| **Machine learning** | 1.000 | 0.745 | 0.534 | 0.449 |
| **Artificial Intelligence** | 0.745 | 1.000 | 0.497 | 0.430 |
| **High energy physics** | 0.534 | 0.497 | 1.000 | 0.601 |
| **Nuclear reactions** | 0.449 | 0.430 | 0.601 | 1.000 |

## 🔧 Технические детали

### Модели

**LLM для извлечения тегов:**
- Провайдер: OpenRouter API
- Модель: `openai/gpt-4o-mini`
- Температура: 0.0 (детерминированность)

**Эмбединги:**
- Модель: `BAAI/bge-base-en-v1.5`
- Размерность: 768
- Префикс: "Scientific theme: {tag}" (для контекста научной тематики)
- Нормализация: включена (для косинусного сходства)
- Устройство: MPS (Mac M1) / CUDA / CPU (автоматический выбор)

**Кластеризация:**
- Метод: Hierarchical Agglomerative Clustering
- Метрика: Cosine distance
- Linkage: Average (UPGMA)
- Алгоритм названий: Weighted Tag Frequency
- Визуализация: Dendrogram + UMAP (2D)

### Оптимизация памяти

1. **Batch processing** — обработка тегов батчами (16 тегов)
2. **float32** вместо float64 (экономия 50% памяти)
3. **Compressed .npz** формат (сжатие NumPy)
4. **Streaming** — построчная обработка JSONL файлов

### Retry механизм

Все запросы к OpenRouter API имеют автоматические повторы (tenacity):
- Exponential backoff (1-60 секунд)
- До 10 попыток
- Обработка пустых ответов и ошибок сети

### Алгоритм названий кластеров (Weighted Tag Frequency)

Для каждого кластера:
1. Собираем все теги из документов кластера
2. Суммируем веса тегов: `tag_score[tag] = Σ(weight_i)` по всем документам
3. Сортируем теги по убыванию суммарного веса
4. Название кластера = топ-3 тега через запятую

**Пример:**
```
Документы кластера:
  Doc 0: {"ML": 0.6, "NN": 0.3, "Opt": 0.1}
  Doc 1: {"ML": 0.5, "DL": 0.4, "AI": 0.1}
  Doc 2: {"NN": 0.7, "DL": 0.2, "Vision": 0.1}

Суммарные веса:
  ML: 1.1, NN: 1.0, DL: 0.6, AI: 0.1, Opt: 0.1, Vision: 0.1

Название: "Machine learning, Neural networks, Deep learning"
```

## 📚 Рекомендуемые источники данных

- **lens.org** — патенты и научные публикации
- **wipo** — международные патенты
- **espacenet** — европейские патенты
- **oliis.jinr.ru** — данные ОИЯИ
- **fips** — российские патенты
- **Git-репозитории** — программное обеспечение

## 🎯 Формат сдачи (хакатон)

### Отчёт должен включать:

1. **Методология исследования:**
   - ✅ Описание пайплайна обработки (5 этапов)
   - ✅ Используемые модели (GPT-4o-mini + BAAI/bge-base-en-v1.5)
   - ✅ Алгоритмы (Weighted Tag Frequency, Hierarchical Clustering)
   - ✅ Обработка данных (JSONL → Embeddings → Clustering)

2. **Аналитическая часть:**
   - ✅ Статистика: 10 документов, 27 уникальных тегов, 3 кластера
   - ✅ Распределение по научным областям:
     - AI/ML (6 документов)
     - High Energy Physics (3 документа)
     - Distributed Computing (1 документ)
   - ✅ Метрики кластеризации (Silhouette: 0.334, Davies-Bouldin: 1.037)

3. **Результаты и выводы:**
   - ✅ Дендрограмма (визуализация иерархии кластеров)
   - ✅ UMAP 2D визуализация (разделение научных областей)
   - ✅ Названия кластеров (автоматические, на основе весов тегов)
   - ✅ Интерактивная 3D визуализация (веб-приложение)
   - 🔄 PDF-отчёт с аналитикой (опционально)

## 📊 Текущее состояние проекта

### Реализовано полностью (5 из 5 этапов) ✅

- ✅ **ЭТАП 0:** Присвоение ID документам
- ✅ **ЭТАП 1:** Извлечение научных тегов с помощью LLM
- ✅ **ЭТАП 2:** Генерация эмбедингов тегов (с префиксом "Scientific theme:")
- ✅ **ЭТАП 3:** Генерация эмбедингов документов (weighted sum)
- ✅ **ЭТАП 4:** Иерархическая кластеризация + визуализация
- ✅ **ЭТАП 5:** Интерактивная 3D визуализация патентного ландшафта

### Особенности визуализации

**Интерактивная 3D визуализация:**
- **Технологии:** Dash + Plotly + Bootstrap для веб-интерфейса
- **3D навигация:** вращение, масштабирование, перемещение в реальном времени
- **Кэширование:** 79 уровней кластеризации (~800 MB) для мгновенного переключения
- **Интерфейс:** 3 панели (список кластеров 25% + 3D визуализация 50% + настройки 25%)
- **Настройки:**
  - Логарифмический слайдер количества кластеров (1-1948)
  - Слайдер масштаба сфер (10-200)
  - Выбор формулы радиуса: `√n×log(n)`, `∛n`, или `log(n)`
- **Интерактивность:**
  - Клик на кластер → детальная информация (топ-5 тегов, количество документов)
  - Список всех кластеров с цветами и названиями
- **Наследование цветов:** от родительских кластеров в иерархии
- **Адаптивные названия:** 1 тег (≥10 кластеров), 2 тега (5-9), 3 тега (<5)

### Файлы и артефакты

**Код:**
- ✅ 8 основных модулей в `src/` + 2 модуля визуализации
- ✅ 3 тестовых скрипта в `temporary_scripts/`
- ✅ Конфигурация в `config.json`
- ✅ Системный промпт в `prompt.txt`

**Данные:**
- ✅ 1948 документов с ID (полный датасет)
- ✅ Эмбединги документов (1948 × 1024)
- ✅ Кэш кластеризации (~800 MB, 79 уровней)

**Визуализации:**
- ✅ Дендрограмма (`dendrogram.png`)
- ✅ UMAP 2D проекция (`umap_clusters.png`)
- ✅ Интерактивная 3D визуализация (веб-приложение)

## 👥 Команда

JINR Hackathon Autumn 2025

## 📝 Лицензия

Проект создан для хакатона JINR Hackathon Autumn 2025

---

**🤖 Powered by Claude Code** (claude.ai/code)
